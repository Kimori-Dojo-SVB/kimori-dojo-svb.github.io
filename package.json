{
    "private": true,
    "scripts": {
        "start": "bundle exec jekyll serve --livereload --drafts --open",
        "build": "webpack --mode production --config webpack.config.js",
        "prod": "npm run build",
        "format": "npm run lint -- --fix",
        "postinstall": "bundle config set --local path 'vendor/bundle' && bundle install --jobs=4 --retry=3"
    },
    "dependencies": {},
    "devDependencies": {},
    "browserslist": [
        "> 0.5%",
        "last 2 versions",
        "Firefox ESR",
        "not dead"
    ]
}
