{
    "private": true,
    "scripts": {
        "start": "jekyll serve --livereload --drafts --open",
        "format": "npm run lint -- --fix",
        "postinstall": "bundle config set --local path 'vendor/bundle' && bundle install --jobs=4 --retry=3"
    },
    "dependencies": {},
    "devDependencies": {},
    "browserslist": [
        "> 0.5%",
        "last 2 versions",
        "Firefox ESR",
        "not dead"
    ]
}
